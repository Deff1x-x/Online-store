# Named upstreams for Node rollback and .NET primary.
# Selection is done via koz_active in active-upstream.conf (do not expose these directly to the internet).

upstream koz_dotnet {
    server dotnet-api:8080;
    keepalive 32;
}

upstream koz_node {
    server node-api:3000;
    keepalive 16;
}
